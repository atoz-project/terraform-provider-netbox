---
# generated by https://github.com/fbreckle/terraform-plugin-docs
page_title: "netbox_token Resource - terraform-provider-netbox"
subcategory: "Authentication"
description: |-
  From the official documentation https://docs.netbox.dev/en/stable/rest-api/authentication/#tokens:
  A token is a unique identifier mapped to a NetBox user account. Each user may have one or more tokens which he or she can use for authentication when making REST API requests. To create a token, navigate to the API tokens page under your user profile.
  NetBox 4.5+ Token Versions:
  - v1 tokens: Legacy 40-character tokens. You can specify the key directly.
  - v2 tokens: New format (nbt_.). Leave key empty to let NetBox generate a v2 token. The full token is only shown once at creation time in the NetBox UI.
---

# netbox_token (Resource)

From the [official documentation](https://docs.netbox.dev/en/stable/rest-api/authentication/#tokens):

> A token is a unique identifier mapped to a NetBox user account. Each user may have one or more tokens which he or she can use for authentication when making REST API requests. To create a token, navigate to the API tokens page under your user profile.

**NetBox 4.5+ Token Versions:**
- **v1 tokens**: Legacy 40-character tokens. You can specify the key directly.
- **v2 tokens**: New format (nbt_<KEY>.<SECRET>). Leave key empty to let NetBox generate a v2 token. The full token is only shown once at creation time in the NetBox UI.

## Example Usage

```terraform
resource "netbox_user" "test" {
  username = "johndoe"
  password = "Abcdefghijkl1"
}

resource "netbox_token" "test_basic" {
  user_id       = netbox_user.test.id
  key           = "0123456789012345678901234567890123456789"
  allowed_ips   = ["2.4.8.16/32"]
  write_enabled = false
  expires       = "2036-01-02T15:04:05.000Z"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `user_id` (Number)

### Optional

- `allowed_ips` (List of String)
- `description` (String)
- `expires` (String)
- `key` (String, Sensitive) For v1 tokens: specify a 40-character key. For v2 tokens (NetBox 4.5+): leave empty to let NetBox generate the token. Note: v2 token plaintext is only available at creation time in NetBox UI.
- `write_enabled` (Boolean)

### Read-Only

- `id` (String) The ID of this resource.
- `last_used` (String)


